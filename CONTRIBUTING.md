# 🤝 BUT Network में योगदान करें

## 🚀 शुरुआत

```bash
# Fork + Clone
git clone https://github.com/sanjaynain754/Butcoin.git
cd Butcoin

# Branch बनाएँ
git checkout -b feature/your-feature

# Setup
cd but-crypto && cargo build
cd ../but-p2p && go build
cd ../but-wallet && flutter pub get
